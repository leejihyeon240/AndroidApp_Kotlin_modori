<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.guruapp">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/appicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/appicon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GuruApp">
        <receiver android:name=".AlarmReceiver"
            android:exported="false"/>
        <activity android:name=".PortfolioActivity2"></activity>
        <activity android:name=".PortfolioActivity" />
        <activity android:name=".alarm" />
        <activity android:name=".stopwatch" />
        <activity android:name=".cal" />
        <activity android:name=".play" />
        <activity
            android:name=".tel"/>
        <activity android:name=".site" />
        <activity android:name=".mainpage" />
        <activity
            android:name=".RegisterActivity"
            android:parentActivityName=".Login" /> <!-- <activity android:name=".Login" /> -->
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>